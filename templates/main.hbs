{{> precheck}}
{{> joinclass}}
<div id="virtualclassApp" style="display: flex;" class="try-to-connect">
 {{#if isPlay}}
    {{> recordingControl}}
  {{/if}}
  {{> leftBar}}
  {{> rightBar }}
  {{> popupCont}}
  {{> dashboardCont}}
</div>