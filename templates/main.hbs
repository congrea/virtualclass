{{> precheck}}
<div id="virtualclassApp" style="display: block;" class="try-to-connect">
 {{#if isPlay}}
    {{> recordingControl}}
  {{/if}}
  {{> rightBar }}
  {{> leftBar}}
  {{> popupCont}}
  {{> dashboardCont}}
</div>