{{> precheck}}
{{> joinclass}}
<div id="virtualclassApp" class="openRightbar" style="display: flex;" class="try-to-connect">
 {{#if isPlay}}
    {{> recordingControl}}
  {{/if}}
  {{> leftBar}}
  {{> rightBar }}
  {{> popupCont}}
  {{> dashboardCont}}
</div>