{{> precheck}}
{{> joinclass}}
{{> mobileLandscapeWarn}}
<div id="virtualclassApp" class="openRightbar" style="display: flex;" class="try-to-connect">
 {{#if isPlay}}
    {{> recordingControl}}
  {{/if}}
  {{> leftBar}}
  {{> rightBar }}
  {{> footerBar}}
  {{> popupCont}}
</div>